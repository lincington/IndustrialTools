<Window x:Class="IndustrialTools.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Loaded="Window_Loaded"
        xmlns:core="clr-namespace:IndustrialTools.Core;assembly=IndustrialTools.Core"
        Background="LightGray"   WindowStartupLocation="CenterScreen"    
        WindowState="Maximized"   ShowInTaskbar="True" ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" 
        Title="{Binding Title}" Height="350" Width="525" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ContentControl   Grid.Row="0"  prism:RegionManager.RegionName="{x:Static core:RegionNames.MenuRegion}" />
        <ContentControl   Grid.Row="1"  prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}" />
    </Grid>
</Window>
